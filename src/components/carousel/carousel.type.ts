export enum Direction {
  Horizontal = 'horizontal',
  Vertical = 'vertical',
}

export enum AnimationName {
  Slide = 'slide',
  Fade = 'fade',
}

export enum ArrowInMode {
  Always = 'always',
  Hover = 'hover',
  Never = 'never',
}

export enum IndicatorPosition {
  Tob = 'top',
  Bottom = 'bottom',
  Left = 'left',
  Right = 'right',
}
